# Setup
title:               carlwilsn
tagline:             'DeepDL log — Summer 2026'
description:         'Reproducing BitNet b1.58 inside pytorch/torchtitan. Weekly notes from the build.'
url:                 https://carlwilsn.github.io
baseurl:             ''
permalink:           /:title/

author:
  name:              Carl Wilson
  url:               https://github.com/carlwilsn

future:               true   # show posts dated in the future

# Build: source is the parent dir (the repo root). The workflow copies
# root-level `posts/`, `open-questions/`, and `revelations/` into their
# Jekyll folders (`_posts/`, `_open_questions/`, `_revelations/`) before
# jekyll runs.

collections:
  open_questions:
    output: false
  revelations:
    output: false
