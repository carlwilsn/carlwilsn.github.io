source "https://rubygems.org"

gem "jekyll", "~> 4.3"

# Ruby 3.4+ ships without these by default; Jekyll still uses them.
gem "csv"
gem "base64"
gem "logger"

# WEBrick (Ruby 3.0+ doesn't ship it; jekyll serve needs it)
gem "webrick", "~> 1.8"
